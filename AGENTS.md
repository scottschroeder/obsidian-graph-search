<project_instructions>

  <xtask>
  # Xtask

  when running `xtask`, you must invoke it as
  ```
  cargo run --package xtask -- <subcommand>
  ```
  </xtask>


  <ci>
    # CI
	Note that you need to specifically check `.github/workflows/*` for github actions. These are hidden files and are easy to miss. But they do exist.
  </ci>
</project_instructions>
